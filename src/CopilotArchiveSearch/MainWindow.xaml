<Window x:Class="Brx.CopilotArchiveSearch.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
        xmlns:ww2="clr-namespace:Microsoft.Web.WebView2.WinForms;assembly=Microsoft.Web.WebView2.WinForms"
        xmlns:wfh="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize"
        Background="{StaticResource BrushDarkBackground}"
        Title="Copilot Archive Search"
        Width="900" Height="600"
        MinWidth="600" MinHeight="200"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

    <Window.Resources>
        <ResourceDictionary>
            <Style x:Key="PanelBorderStyle" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource BrushDarkPanel}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BrushDarkBorder}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="0"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <!-- MAIN CONTENT -->
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search bar -->
        <TextBox x:Name="SearchBox"
                 Grid.Row="0"
                 Tag="Search your Copilot conversations..."
                 TextChanged="SearchBox_TextChanged"/>

        <!-- Results + Preview -->
        <Grid Grid.Row="1" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="200" MaxWidth="400"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ListBox x:Name="ResultsList"
                     Grid.Column="0"
                     Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ColumnDefinition}}"
                     DisplayMemberPath="Display"
                     SelectionChanged="ResultsList_SelectionChanged"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     d:ItemsSource="{d:SampleData ItemCount=50}"/>

            <GridSplitter Grid.Column="1"
                  Width="5"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  ResizeBehavior="PreviousAndNext"
                  ResizeDirection="Columns"/>

            <Border Style="{StaticResource PanelBorderStyle}" Grid.Column="2" Margin="0">
                <wfh:WindowsFormsHost>
                    <ww2:WebView2 x:Name="PreviewBrowser"/>
                </wfh:WindowsFormsHost>
            </Border>
        </Grid>

    </Grid>

</Window>
